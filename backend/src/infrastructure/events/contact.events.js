/**
 * Domain Events — Contact Domain
 */

const CONTACT_EVENTS = {
  CREATED: 'contact.created',
  UPDATED: 'contact.updated',
  TAGGED: 'contact.tagged',
};

module.exports = {
  CONTACT_EVENTS,
};
