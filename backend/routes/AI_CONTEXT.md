# Routes AI Context

Purpose: register endpoint paths and connect them to controllers.

Patterns:
- one router per domain
- avoid business logic in route files

Dependencies:
- Express router
- matching controller module

Conventions:
- keep route declarations declarative and minimal
