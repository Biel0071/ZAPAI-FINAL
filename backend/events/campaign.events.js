/**
 * Domain Events — Campaign Domain
 */

const CAMPAIGN_EVENTS = {
  STARTED: 'campaign.started',
  PROGRESS: 'campaign.progress',
  FINISHED: 'campaign.finished',
  FAILED: 'campaign.failed',
};

module.exports = {
  CAMPAIGN_EVENTS,
};
