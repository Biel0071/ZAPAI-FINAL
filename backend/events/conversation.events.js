/**
 * Domain Events — Conversation Domain
 */

const CONVERSATION_EVENTS = {
  CREATED: 'conversation.created',
  UPDATED: 'conversation.updated',
  CLOSED: 'conversation.closed',
  ASSIGNED: 'conversation.assigned',
};

module.exports = {
  CONVERSATION_EVENTS,
};
