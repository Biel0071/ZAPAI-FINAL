# Production build — requests go through Nginx proxy at /api
# Nginx serves frontend and proxies /api/* to backend:4025
# Never use localhost or absolute IP in production
VITE_API_URL=/api
VITE_APP_VERSION=1.2.0-freeze
VITE_APP_VERSION_LABEL=1.2.0-freeze
VITE_ENABLE_DEMO_DATA=false
