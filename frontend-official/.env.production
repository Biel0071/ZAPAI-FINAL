# Production build — requests go through Nginx proxy (not direct to backend:4025)
# The setup-vps.sh script serves frontend on port 3000 via Nginx.
# Socket.IO also routes through Nginx /socket.io/ proxy.
VITE_API_URL=http://209.50.229.68:3000
