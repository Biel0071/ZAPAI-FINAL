# AI Context — hooks

- **Purpose**: Reusable React hooks for cross-page behavior.
- **Patterns**: Encapsulate stateful logic and browser API bindings behind stable hook APIs.
- **Data Flow**: Hooks expose state + callbacks; pages remain declarative.
- **Dependencies**: React hooks, utility services, app-level providers.
